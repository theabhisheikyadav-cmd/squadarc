# Public URL of the site (used for OG tags and invite links)
NEXT_PUBLIC_SITE_URL=https://squadarc.app

# Supabase (Project Settings → API). Service role key is server-only; never expose it.
# Leave empty in local dev to use an in-memory waitlist.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
